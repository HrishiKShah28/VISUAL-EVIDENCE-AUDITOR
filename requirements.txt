# --- Streamlit + Web UI ---
streamlit==1.39.0
python-dotenv
requests
tqdm
pandas
numpy==1.24.4
plotly

# --- Whisper Transcription ---
faster-whisper==0.10.0
ffmpeg-python==0.2.0

# --- Video + OCR Processing ---
opencv-python-headless==4.8.1.78
pytesseract
imutils

# --- Python Build Fix (for distutils issue on Python 3.13) ---
setuptools>=69.0.0

# --- VideoDB client (install directly from GitHub) ---
videodb @ git+https://github.com/whispernews/videodb-client.git
